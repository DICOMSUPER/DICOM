export enum NotificationType {
  URGENT = 'urgent',
  SYSTEM = 'system',
  INFO = 'info',
  WARNING = 'warning',
  SUCCESS = 'success'
}

export enum NotificationPriority {
  LOW = 'low',
  MEDIUM = 'medium',
  HIGH = 'high',
  CRITICAL = 'critical'
}

export enum NotificationStatus {
  UNREAD = 'unread',
  READ = 'read',
  ARCHIVED = 'archived'
}