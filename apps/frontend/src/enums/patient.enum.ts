export enum QueueStatus {
  WAITING = 'waiting',
  IN_PROGRESS = 'in_progress',
  COMPLETED = 'completed',
  EXPIRED = 'expired',
}

export enum QueuePriorityLevel {
  ROUTINE = 'Routine',
  URGENT = 'Urgent',
  STAT = 'Stat',
}

export enum RoomType {
  CT = "CT",
  WC = "WC",
}
